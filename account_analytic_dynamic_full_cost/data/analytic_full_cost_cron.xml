<odoo>
    <record id="ir_cron_analytic_full_cost_daily_lines" model="ir.cron">
        <field name="name">Analytic dynamic full cost - daily lines</field>
        <field name="model_id" ref="analytic.model_account_analytic_line"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
today = datetime.datetime.today().date()
plans = env['account.analytic.plan']
for company in env['res.company'].search([]):
    plans.recompute_full_cost_multipliers(company=company, date_to=today)
    date_from = plans._get_full_cost_period_start(company, today)
    lines = model.search([
        ('company_id', '=', company.id),
        ('date', '>=', date_from),
        ('date', '<=', today),
    ])
    if lines:
        lines.recompute_full_cost_amount()
        ]]></field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>
</odoo>
